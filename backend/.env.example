# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=legal_user
DB_PASSWORD=legal_password_dev
DB_NAME=legal_hub

# Server
NODE_ENV=development
API_PORT=3000
API_URL=http://localhost:3000

# JWT
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRATION=7d

# Redis
REDIS_URL=redis://localhost:6379

# CORS
CORS_ORIGIN=http://localhost:3001

# Claude API (para agentes IA)
ANTHROPIC_API_KEY=

# ZapSign (para assinatura digital)
ZAPSIGN_API_KEY=

# Trello (para gestão de tasks)
TRELLO_API_KEY=
TRELLO_TOKEN=

# WhatsApp (Baileys)
WHATSAPP_SESSION_ID=legal-hub-session
N8N_WEBHOOK_URL=http://localhost:5678/webhook/whatsapp

# Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
SMTP_FROM=noreply@legal-hub.com
